function sum(a, b) {
  return Number(a) + Number(b);
}

function divide(a, b) {
  if (b === 0) {
    return 0;
  }
  return Number(a) / Number(b);
}

function multiply(a, b) {
  return Number(a) * Number(b);
}

function subtract(a, b) {
  return Number(a) - Number(b);
}